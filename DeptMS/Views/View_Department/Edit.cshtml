@model DeptMS.Models.View_Department

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>View_Department</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.DEPARTMENT_ID)

        @Html.HiddenFor(model => model.COMPANY_ID)

        @Html.HiddenFor(model => model.DEPARTMENT_CODE)

        @Html.HiddenFor(model => model.DEPARTMENT_CNAME)

        @Html.HiddenFor(model => model.DEPARTMENT_ENAME)

        @Html.HiddenFor(model => model.DEPARTMENT_ABBR)

        @Html.HiddenFor(model => model.PART_DEPARTMENT_ID)

        @Html.HiddenFor(model => model.DEPARTMENT_LEADER_ID)

        @Html.HiddenFor(model => model.GL_DEPARTMENT_CODE)

        @Html.HiddenFor(model => model.DEPARTMENT_STATUS)

        @Html.HiddenFor(model => model.DEPT_LEVEL_CODE)

        @Html.HiddenFor(model => model.DEPT_LEVEL_CNAME)

        @Html.HiddenFor(model => model.DEPT_LEVEL_LEVEL)

        @Html.HiddenFor(model => model.ACTIVATE_DATE)

        @Html.HiddenFor(model => model.EXPIRED_DATE)

        @Html.HiddenFor(model => model.MODIFIED_DATE)

        @Html.HiddenFor(model => model.DEPT_LEADER_NO)

        @Html.HiddenFor(model => model.EMPLOYEE_EMAIL_1)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
